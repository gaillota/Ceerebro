Router.map(function() {
    // User's profile
    this.route('/profile', {
        name: 'profile'
    });
    // Change password
    //this.route('/profile/change-password', {
    //    name: 'profile.change.password'
    //});
});
